# Tensor ops (small examples)

Pure tensor operations: reductions, element-wise, no recurrence. ML building blocks.

| File | What it does | Run |
|------|--------------|-----|
| [softmax.ein](softmax.ein) | softmax(x) = exp(x) / sum(exp(x)) | `python3 -m einlang examples/tensor_ops/softmax.ein` |
