# Sum function for mathematical operations
# Provides sum functionality for arrays and tensors

pub fn sum(arr) {
    # Sum all elements in an array
    # Using numpy for efficient array support
    python::numpy::sum(arr)
}
